# Task: Wire up Sign Out in the Seller Dashboard

## Steps
- [x] 1. Update `app/ui/Nav/NavLinks.tsx` to call the `logout()` server action from the "SIGNS OUT" nav item (replace placeholder link with a form/button).
- [x] 2. Verify with lint/typecheck.
